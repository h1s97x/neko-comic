name: neko_ui
description: Reusable UI components for NekoComic
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter

  # UI
  flutter_staggered_grid_view: any
  shimmer_animation: any
  flex_seed_scheme: ^3.0.0
  dynamic_color: any

  # Utils
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
