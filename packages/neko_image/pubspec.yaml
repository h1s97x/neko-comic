name: neko_image
description: Image loading and caching for NekoComic
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.41.0'

dependencies:
  flutter:
    sdk: flutter

  # Storage
  path_provider: any
  path: ^1.9.0

  # Utils
  uuid: ^4.5.1
  mime: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
